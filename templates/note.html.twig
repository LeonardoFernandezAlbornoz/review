{% extends "base.html.twig" %}

{% block title %}
	Note |
	{{ parent() }}
{% endblock %}

{% block body %}

	dd8
	<img src="{{asset('/img/')}}{{img}}" alt="img"></img>
<p>
	<b>Description:
	</b>
	{{note.description}}</p>
<p>
	<b>Created at:
	</b>
	{{note.createdAt|date("d-m-Y")}}
	<span>
		{{prettyDate}}
	</span>

</p>


<b>Category:
</b>
{{note.idCategory.description}}</p><a href="{{path('app_detail',{slug:note.id})}}">
<b>Description:</b>
{{note.description}}</a>{% endblock %}
